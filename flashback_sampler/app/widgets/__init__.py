"""Custom-painted Qt widgets for the Erebus chassis.

Each widget owns its appearance via paintEvent() — Qt stylesheets set
surrounding surfaces but the instruments themselves are custom drawn so
they match the frontend-design skill's component specs.
"""
