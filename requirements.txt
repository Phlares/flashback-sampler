numpy>=1.24
sounddevice>=0.5.0
soundfile>=0.12.1
soundcard>=0.4.3  ; sys_platform == "win32"  # WASAPI loopback on Windows
PySide6>=6.7

# dev — install with `pip install -e ".[dev]"` or directly:
# pytest>=8.0
# pytest-cov>=5.0
# pytest-timeout>=2.3

# Raspberry Pi only — install manually on Pi:
#   pip install RPi.GPIO
